# Satella 更新历史（CHANGELOG）

按 git 提交记录整理（哈希为短哈希，新在上旧在下）。

> 本项目（含本文档）的开发与维护使用了 **GPT、DeepSeek、GLM** 三个 AI 模型协作完成。

## 自动交易

- `99c9d2c` 交易按间隔重查背包在已开界面续买；新增「刷新交易界面」配置（0~1200 gt，默认0）定期关容器清村民占用；成交前清理光标残余并修复盔甲/副手槽越界崩溃
- `5759f17` 交易目标改用 UUID 跨重进跟踪；交易热键界面可用并新增交易 GUI 开关
- `bc974dc` 新增拦截目标物品丢弃（白名单语义，后续 `4ad693d` 细化为仅允许光标移出界面丢弃）

## 全自动合成 / 自动化

- `5759f17` 合成补料修复早退游标跳槽（回滚游标到上次检查槽位再返回）
- `4bf6f38` 合成补料材料表改为按数量记账：按缺口补满到最大堆叠，半堆不超过缺口才取，去掉放回原堆逻辑
- `0e73f5a` 合成扫描改为先建材料表再查表补料：拾起到光标即核销缺口，剩余材料可续填多个格子；新增 GUI 显示开关与 GUI 内可用的自动化热键
- `76eeb4f` 配置界面改三分类（交易/自动化/杂项）并固定标题；新增全自动切石；合成与切石合并为自动化开关/模式
- `134dc55` 自动合成材料扫描改为共享环形游标：一圈无收获不提前收工仍摊匀清理，周期配置改为默认3范围1~100
- `7a21d22` 按 Item Scroller 逻辑循环取出全自动合成结果
- `fa9f51d` 调整自动合成清理为丢弃并将合成残余上限提高至32
- `210b153` / `4f0b8b9` 将残差合成并入全自动合成，新增独立合成周期配置
- `42df0d2` 新增「合成残余」配置（0~16）：背包扫描时物品堆数量 0.5*P < 残余值则跳过

## 物品保护与杂项

- `4ad693d` 拦截目标物品丢弃改为白名单语义：THROW 落槽位全拦截，slotId=-999 放行
- `32ea0aa` / `0bec3c9` / `4375884` 修复 DropBlockCreativeScreenMixin 在 1.21.11 的目标查找与重映射问题
- `e019bd1` 修复附魔显示颜色与 ETF 附魔像素点不适配：entity glint 层改回原版 GLINT 管线
- `6449b00` 更NB的弩：容器打开或 litematica-printer 自动补货期间暂停连射

## /st 定位器

- `19db9fe` 数据包加载回退：仅加载默认启用的数据包
- `3b05189` / `48ea66d` / `1fd8317` 世界生成栈、群系搜索与结构搜索的诊断日志
- `ba4c50e` 重新打包 Satella.jar（含 wrapper 与双版本嵌套 jar）
- `58cf3d1` 多环定位未命中环规则的默认配置改为加载全部数据包
- `da04442` 航点串消息支持点击导入
- `5902c14` st 查询结果附加 Xaero 航点串输出
- `bba581b` 新增 `/st anystructure` 与 `/st anybiome`：就近多结构/多群系查询
- `6f127ad` 修复 `/st rules` 从聊天栏打开编辑器被聊天框关闭覆盖
- `6c7087e` st rules 指令执行时写日志
- `a4c0bd2` 多环定位规则编辑器改为 `/st rules` 指令打开，数据包改为勾选列表
- `4972b22` 配置了多环规则时不再强制要求 `/st seed` 全局种子
- `d62e75d` 新增多环定位规则可视化编辑器（参考 LPCTools 的 MaLiLib 自定义屏幕模式）
- `5ba9872` 新增多环定位：MaLiLib 配置切比雪夫距离环 → 种子 + 数据包子集
- `025b26d` 修复 `/st` 维度选择：原版 overworld 定义在 world preset 中
- `12c6105` 修复 `/st structure` 搜索区域坍缩：getStartChunk 入参应为区块坐标
- `8e53330` 修复 `/st` 注册表接线：worldgen/dimension/静态层分流查找
- `e911713` 修复 `/st` 注册表重复加载
- `c8ebd6a` 修复 `/st` 加载带 lithostitched 的数据包
- `91946a8` 修复 `/st reload`：注册表逐元素容错加载，session 持久化避免锁冲突
- `0884300` 完善 `/st`：群系/结构指令自动补全，reload 汇报加载的数据包
- `f7428a8` 新增纯客户端 `/st` 群系与结构定位器（移植 Datapack Map 算法）

## 构建与打包

- `2815ad2` 回退到基础版并重新编译双版本 Satella
- `1e2f1dd` 重命名为 Satella 并打包双版本（1.21.11 + 26.2）单 jar
- `f8e451d` updated source to github page
- `6a6d0dd` Update README.md
- `61d0c7e` init
